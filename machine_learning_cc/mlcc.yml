# type the below two lines into your terminal:
# conda env create --file machine_learning_cc/mlcc.yml
# conda activate mlcc

name: mlcc

dependencies:
  - python
  - tensorflow
  - jupyter
  - ipython
  - matplotlib
  - pandas
  - scikit-learn
  - scipy
  - seaborn

# to verify environments:
# conda info --envs

# to remove environment:
# conda env remove -n mlcc

# If you update the `mlcc.yml` you can run the following to update the
# environment with your changes:
# conda env update --file machine_learning_cc/mlcc.yml --prune